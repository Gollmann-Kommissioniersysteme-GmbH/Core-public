name: Wiki Change to Discussion

on:
  gollum  # This event triggers when the wiki is updated

jobs:
  post-discussion:
    runs-on: ubuntu-latest

    steps:
      - name: Extract Repo Information
        run: |
          OWNER="${{ github.repository_owner }}"
          REPO_NAME="$(basename ${{ github.repository }})"
          echo "Repository: $OWNER/$REPO_NAME"

      - name: Create Discussion
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          OWNER="${{ github.repository_owner }}"
          REPO_NAME="$(basename ${{ github.repository }})"
          ACTOR="${{ github.actor }}"
          WIKI_URL="https://github.com/$OWNER/$REPO_NAME/wiki"
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")

          # ✅ Fetch Repository ID (Fix)
          REPO_ID=$(gh api repos/$OWNER/$REPO_NAME --jq '.id')

          # ✅ Get Discussion Category ID (Default: General)
          CATEGORY_ID=$(gh api graphql -f query='
            query {
              repository(owner: "'"$OWNER"'", name: "'"$REPO_NAME"'") {
                discussionCategories(first: 10) {
                  nodes { id, name }
                }
              }
            }' | jq -r '.data.repository.discussionCategories.nodes[] | select(.name=="General") | .id')

          if [[ -z "$CATEGORY_ID" ]]; then
            echo "Error: No valid discussion category found!"
            exit 1
          fi

          # ✅ Create Discussion
          DISCUSSION_TITLE="Wiki Updated by $ACTOR"
          DISCUSSION_BODY="📖 **Wiki Page Updated**  
          - **User:** $ACTOR  
          - **Time:** $TIMESTAMP  
          - **[View Wiki]($WIKI_URL)**  

          _This discussion was automatically created by GitHub Actions._"

          gh api graphql -f query='
            mutation($repositoryId: ID!, $categoryId: ID!, $title: String!, $body: String!) {
              createDiscussion(input: {repositoryId: $repositoryId, categoryId: $categoryId, title: $title, body: $body}) {
                discussion {
                  url
                }
              }
            }' -F repositoryId="$REPO_ID" -F categoryId="$CATEGORY_ID" -F title="$DISCUSSION_TITLE" -F body="$DISCUSSION_BODY"
